name: Deploy to GitHub Pages (Disabled)

# This workflow is now disabled as the repository is archived
on:
  # Removed push trigger to disable automatic deployments
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Display Archive Notice
        run: |
          echo "This repository is archived and deployments are disabled."
          echo "Please visit https://shahmeetk.github.io for the current portfolio."

      # Deployment step is commented out as the repository is archived
      # - name: Deploy to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: .
      #     force_orphan: true
      #     cname: shahmeetk.github.io
